import React from "react";
import styled from "styled-components";

const Login = () => {
  return <MainCont className="w-screen h-screen flex flex-col">Login</MainCont>;
};

export default Login;

const MainCont = styled.div`
  background: #900;
  align-items: center;
  justify-content: center;
`;
